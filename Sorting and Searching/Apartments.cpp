#include <bits/stdc++.h>
using namespace std;

int main(){
	int n, m, k, count;
	cin >> n >> m >> k;
	int a[n], b[m];
	for(int i = 0; i < n; ++i){
		cin >> a[i];
	}
	for(int i = 0; i < m; ++i){
		cin >> b[i];
	}
	for(auto x : a){
		if()
	}
	cout << count;
	return 0;
}