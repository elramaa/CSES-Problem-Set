#include <bits/stdc++.h>
using namespace std;

int main(){
	string str;
	int count, max;
	char prev;
	// char current = '';
	cin >> str;
	for(int i=0; i<str.length(); ++i){
		if(prev == str[i]){
			
		}
		max = max()
	}
}